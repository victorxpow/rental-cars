<%= form_with model: @client, class: 'ls-form ls-form-horizontal row', local: true do |f| %>
  <div class = 'ls-label col-md-12'>
    <p>
        <%= f.label :name %>
        <%= f.text_field :name, placeholder:'João Victor' %>
    </p>
    <p>
        <%= f.label :cpf %>
        <%= f.text_field :cpf, placeholder: '123.456.789-10'  %>
    </p>
    <p>
        <%= f.label :email %>
        <%= f.text_field :email, placeholder: 'fulano@email.com' %>
    </p>
  </div>
  <div class="button">
    <div class='ls-actions-btn button'>
        <%= f.submit 'Salvar', class: 'ls-btn', style: "color: rgb(79, 89, 216);" %>
        <%= link_to 'Voltar', url_for(:back), class:"ls-btn" %>
    </div>
  </div>
<% end %>